package me.nickpierson.StatisticsSolver.pc;

public class PCCalculator {
	
	/* IDEA:
	 * Simple class for holding all the different data
	 * Step 1) Initialize class. It does nothing in constructor.
	 * Step 2) Set everything it needs.
	 * Step 3) Call calculate()
	 * Step 4) Pass to view. Call getX() for whatever you need
	 * 	and this class will always return a string of either the desired value
	 * 	or "n/a".
	 * 
	 * JUST KIDDING... just do this in the model.
	 * 
	 * model.setN();
	 * model.setR();
	 * ...
	 * getFormattedNFact() - returns NFact or "default" return value
	 * getFormattedPermutation()
	 * etc...
	 * 
	 * if(view.hasN){
	 * 
	 * if(view.hasR)
	 * 
	 * if(view.hasN && hasR)
	 * 
	 * if(view.hasN && hasNs)
	 * 
	 * no else
	 */
	
}
